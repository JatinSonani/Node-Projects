<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit To-Do Task</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            background-image: url(https://images3.alphacoders.com/756/756396.jpg);
        }
        .container {
            margin-top: 150px;
        }
        .card {
            border-radius: 15px;
        }
        .deadline-label {
            color: #dc3545;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card shadow-sm">
            <div class="card-body">
                <h1 class="text-center mb-4">Edit Task</h1>
                <form action="/updateusers" method="post">
                    <input type="hidden" name="editId" value="<%= single.id %>">
                    <div class="mb-3">
                        <label for="name" class="form-label fw-bold">Task Name</label>
                        <input type="text" name="name" id="name" class="form-control" value="<%= single.name %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="phone" class="form-label fw-bold">Status</label>
                        <input type="text" name="phone" id="phone" class="form-control" value="<%= single.phone %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="date" class="form-label deadline-label ">Deadline</label>
                        <input type="datetime-local" name="date" id="date" class="form-control" value="<%= single.date %>" required>
                    </div>
                    <button type="submit" class="btn btn-primary">Update Task</button>
                    <a href="/" class="btn btn-secondary">Cancel</a>
                    
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
