<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Edit Book</title>
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
            rel="stylesheet">
    </head>
    <style>
        body {
            color: white;
            background-image: url(https://images.alphacoders.com/145/145201.jpg);
            background-size: cover; 
            background-position: center; 
            background-repeat: no-repeat; 
            background-attachment: fixed; 
            height: 100%; 
            margin: 0;
            font-family: Georgia, 'Times New Roman', Times, serif;
            }
        .form-label{
            font-size: 25px;
             }
             .btn{
                border: rgb(175, 19, 19);
             }
    </style>
    <body>
        <div class="container mt-5">
            <h2 class="text-center">Edit Book</h2>
            
            <!-- Display Book ID -->
            

            <form action="/updateBook/<%= book._id %>" method="POST">
                <div class="mb-3 text-center">
                    <p class="fw-bold fs-4 d-none">Book ID: <%= book._id %></p>
                </div>
                <div class="mb-3">
                    <label for="name" class="form-label">Book Name</label>
                    <input type="text" class="form-control" id="name"
                        name="book_name" value="<%= book.book_name %>" required>
                </div>
                <div class="mb-3">
                    <label for="price" class="form-label">Book Price</label>
                    <input type="number" class="form-control" id="price"
                        name="book_price" value="<%= book.book_price %>" required>
                </div>
                <div class="mb-3">
                    <label for="pages" class="form-label fw-bold">Pages</label>
                    <select class="form-select" id="pages" name="book_pages" required>
                        <option value="">---- Select Pages ----</option>
                        <option value="100-200" <%= book.book_pages === '100-200' ? 'selected' : '' %>>100-200</option>
                        <option value="200-300" <%= book.book_pages === '200-300' ? 'selected' : '' %>>200-300</option>
                        <option value="300-400" <%= book.book_pages === '300-400' ? 'selected' : '' %>>300-400</option>
                        <option value="400-500" <%= book.book_pages === '400-500' ? 'selected' : '' %>>400-500</option>
                    </select>
                </div>
                
                <div class="mb-3">
                    <label for="author" class="form-label">Author</label>
                    <input type="text" class="form-control" id="author"
                        name="book_author" value="<%= book.book_author %>" required>
                </div>
                <div class="text-center">
                    <button type="submit"
                        class="btn btn-primary mt-3 fs-5 fw-bold px-4 py-2 ">Update</button>
                </div>
            </form>
        </div>
    </body>
</html>
