<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rustic Bean Blog - Colorful Design</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <style>
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background-image: url(https://wallpapercave.com/wp/wp9980513.jpg);
            color: #333;
        }

        main {
            flex: 1;
        }

        h2 {
            font-family: 'Georgia', Times, serif;
            text-transform: uppercase;
            letter-spacing: 2px;
            color: #3e64ff;
        }

        .card {
            border: none;
            overflow: hidden;
            border-radius: 15px;
            background-color: #f3f4f6;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:nth-child(1) {
            background-color: #ffe4e1;
        }

        .card:nth-child(2) {
            background-color: #e6e6fa;
        }

        .card:nth-child(3) {
            background-color: #f0fff0;
        }

        .card:nth-child(4) {
            background-color: #ffffe0;
        }

        .card:nth-child(5) {
            background-color: #f0f8ff;
        }

        .card:nth-child(6) {
            background-color: #fafad2;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 12px 25px rgba(0, 0, 0, 0.3);
        }

        .card-title {
            font-size: 1.4rem;
            font-weight: bold;
            color: #ff6f61;
        }

        .card-text {
            font-size: 1rem;
            line-height: 1.5;
            color: #555;
        }

        .btn-outline-primary {
            color: #ff6f61;
            border-color: #ff6f61;
        }

        .btn-outline-primary:hover {
            background-color: #ff6f61;
            color: white;
        }

        .btn-outline-danger {
            color: #d72638;
            border-color: #d72638;
        }

        .btn-outline-danger:hover {
            background-color: #d72638;
            color: white;
        }

        .btn-logout {
            background: linear-gradient(45deg, #d72638, #ff6f61);
            color: white;
            border: none;
            transition: background 0.3s ease;
        }

        .btn-logout:hover {
            background: linear-gradient(45deg, #ff6f61, #d72638);
            color: #fff;
        }

        footer {
            background: linear-gradient(120deg, #3e64ff, #764ba2);
            color: white;
        }

        footer p {
            margin: 0;
            font-size: 0.9rem;
        }

        .header-link {
            font-size: 1rem;
            color: #fff;
            margin-left: 1rem;
            transition: color 0.3s;
        }

        .header-link:hover {
            color: #ff6f61;
        }

        .social-icon {
            color: white;
            transition: color 0.3s;
        }

        .social-icon:hover {
            color: #ff6f61;
        }
    </style>
</head>

<body>
    <header class="bg-gradient text-white py-3 shadow-sm" style="background: linear-gradient(45deg, #3e64ff, #764ba2);">
        <div class="container d-flex justify-content-between align-items-center">
            <a href="/" class="logo">
                <img src="/assets/admin/layout/img/brand logo-2.png" alt="Logo" height="50">
            </a>
            <div class="d-flex align-items-center">
                <a href="https://www.facebook.com/" class="social-icon me-3"><i class="fab fa-facebook-f fs-4"></i></a>
                <a href="https://x.com/" class="social-icon me-3"><i class="fab fa-twitter fs-4"></i></a>
                <a href="https://www.linkedin.com/" class="social-icon me-4"><i class="fab fa-linkedin-in fs-4"></i></a>
                <a href="/addblogpage" class="btn btn-light me-2">Add Blog</a>
                <a href="/logout" class="btn btn-logout">Log Out</a>
                
            </div>
        </div>
    </header>

    <main class="py-5">
        <div class="container">
            <h2 class="text-center mb-5">Our Blog</h2>
            <div class="row g-4">
                <% blogs.forEach(blog => { %>
                    <div class="col-md-6 col-lg-4">
                        <div class="card h-100">
                            <img src="<%= blog.image %>" alt="<%= blog.title %>" class="card-img-top" style="height: 500px; object-fit: cover;">
                            <div class="card-body text-center">
                                <h5 class="card-title"><%= blog.title %></h5>
                                <p class="card-text"><%= blog.desc.slice(0, 50) %>

                                </p>
                                <a href="/edit?id=<%= blog.id %>" class="btn btn-outline-primary btn-sm">Edit</a>
                                <a href="/delete?id=<%= blog.id %>" class="btn btn-outline-danger btn-sm">Delete</a>
                                <a href="/readmore?id=<%= blog.id %>" class="btn btn-outline-danger btn-sm">readmore</a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
    </main>

    <footer class="text-center py-4">
        <p>© 2024 Rustic Bean Blog. All Rights Reserved.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
